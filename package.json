{
  "name": "herdr",
  "private": true,
  "type": "module",
  "workspaces": [
    "web",
    "mobile",
    "shared"
  ],
  "scripts": {
    "check": "npm run check -w @herdr/shared && npm run check -w herdr-web-ui",
    "test": "npm run test:unit && npm run test:server",
    "test:unit": "npm run test -w @herdr/shared && npm run test -w capra",
    "test:server": "node --test test/*.test.mjs",
    "test:e2e": "playwright test -c e2e/playwright.config.mjs",
    "test:e2e:ui": "playwright test -c e2e/playwright.config.mjs --ui",
    "test:all": "npm run test:unit && npm run test:server && npm run test:e2e",
    "dev:web": "npm run dev -w herdr-web-ui",
    "dev:mobile": "npm run start -w capra",
    "build:web": "npm run build -w herdr-web-ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
